---
phase: 03-unraid-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [unraid-template/openclaw.xml]
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Icon URL in template resolves to a valid PNG image (HTTP 200)"
    - "Volume Target attribute has no leading space ('/home/node/.openclaw' not ' /home/node/.openclaw')"
    - "Repository field matches actual DockerHub image name"
  artifacts:
    - path: "unraid-template/openclaw.xml"
      provides: "Corrected Unraid template with working icon, clean volume path, verified repository"
      contains: 'Target="/home/node/.openclaw"'
  key_links:
    - from: "unraid-template/openclaw.xml"
      to: "DockerHub image"
      via: "Repository field"
      pattern: "/openclaw"
    - from: "unraid-template/openclaw.xml"
      to: "Icon PNG"
      via: "Icon field URL"
      pattern: "https://.*\\.png"
---

<objective>
Fix three verification gaps in the Unraid XML template: broken icon URL (404), leading space in volume Target path, and unverified DockerHub repository name.

Purpose: Template must have correct values for Community Apps submission and reliable Unraid installation.
Output: Corrected unraid-template/openclaw.xml with all fields validated.
</objective>

<execution_context>
@/Users/benglasser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benglasser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unraid-integration/03-VERIFICATION.md
@unraid-template/openclaw.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix volume Target leading space and find working icon URL</name>
  <files>unraid-template/openclaw.xml</files>
  <action>
    1. Fix the volume Config entry Target attribute: change `Target=" /home/node/.openclaw"` to `Target="/home/node/.openclaw"` (remove leading space).

    2. Find a working icon URL for OpenClaw. Try these approaches in order:
       a. Check if https://raw.githubusercontent.com/OpenClaw/OpenClaw/main/packages/control/public/icon-512.png exists (curl -sI, check for 200)
       b. Search the OpenClaw GitHub repo for PNG icons: try paths like `/public/`, `/assets/`, `/static/`, common icon locations
       c. Check if the repo has a different branch name (master vs main)
       d. If no upstream icon found, check if this repo (openclaw-docker) has an icon committed, or note that one needs to be created/hosted

    3. If a working icon URL is found, update the Icon field in openclaw.xml. If not, leave the current URL and document the finding for the checkpoint task.

    Do NOT change the Repository field yet -- that requires user confirmation.
  </action>
  <verify>
    - `grep 'Target="/home/node/.openclaw"' unraid-template/openclaw.xml` returns a match (no leading space)
    - `grep 'Target=" ' unraid-template/openclaw.xml` returns NO match (no leading spaces in any Target)
    - If icon URL was updated: `curl -sI <new-url> | head -1` returns HTTP 200
  </verify>
  <done>Volume Target path has no leading space. Icon URL either resolves to valid PNG or is documented as needing resolution in checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed volume path leading space in XML template. Attempted to find working icon URL for OpenClaw.</what-built>
  <how-to-verify>
    1. **DockerHub username:** Confirm your DockerHub username. The template currently shows `brglasser/openclaw`. Is this correct, or should it be a different username (e.g., `benglasser/openclaw`)?
       - Check: `docker login` or visit https://hub.docker.com/ to confirm your username
       - If wrong, tell me the correct username

    2. **Icon URL:** Verify the icon URL in the template resolves. If Task 1 found a working URL, confirm it looks right. If not, we need to either:
       a. Find an existing OpenClaw icon to reference
       b. Host an icon in this repository and reference via raw.githubusercontent.com URL
       c. Use a placeholder icon URL temporarily

    3. **Volume path:** Confirm the volume Target is now `/home/node/.openclaw` (no leading space) by checking the XML file.
  </how-to-verify>
  <resume-signal>Provide: (1) correct DockerHub username, (2) icon URL decision, (3) "approved" for volume path fix. Then I will update the template accordingly.</resume-signal>
</task>

</tasks>

<verification>
After checkpoint resolution:
- `xmllint --noout unraid-template/openclaw.xml 2>&1` shows valid XML (or manual inspection confirms well-formed XML)
- Icon URL returns HTTP 200 when curled
- Repository field matches actual DockerHub image name
- No leading spaces in any Target attributes
</verification>

<success_criteria>
All three verification gaps from 03-VERIFICATION.md are resolved:
1. Icon URL resolves to valid PNG (not 404)
2. Volume Target has no leading space
3. Repository name confirmed correct
Template is ready for Phase 4 Community Apps submission.
</success_criteria>

<output>
After completion, create `.planning/phases/03-unraid-integration/03-03-SUMMARY.md`
</output>
