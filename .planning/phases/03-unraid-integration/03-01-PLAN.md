---
phase: 03-unraid-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - unraid-template/openclaw.xml
  - entrypoint.sh
  - Dockerfile
autonomous: false

must_haves:
  truths:
    - "XML template contains all required Unraid fields (Repository, WebUI, Category, Icon, Support)"
    - "Template configures port 18789 as user-editable with correct WebUI format"
    - "Template maps volume to /mnt/user/appdata/openclaw/ targeting container /home/node/.openclaw"
    - "Template exposes PUID/PGID as advanced-hide variables with Unraid defaults (99/100)"
    - "Container entrypoint remaps node user UID/GID to match PUID/PGID environment variables"
    - "API key is optional and masked in template"
  artifacts:
    - path: "unraid-template/openclaw.xml"
      provides: "Unraid Docker template for Community Applications"
      contains: "Container version=\"2\""
    - path: "entrypoint.sh"
      provides: "PUID/PGID-aware entrypoint with UID/GID remapping"
      contains: "PUID"
    - path: "Dockerfile"
      provides: "Updated Dockerfile with gosu for UID remapping"
      contains: "gosu"
  key_links:
    - from: "unraid-template/openclaw.xml"
      to: "entrypoint.sh"
      via: "PUID/PGID environment variables passed through template Config entries"
      pattern: "Target=\"PUID\""
    - from: "entrypoint.sh"
      to: "Dockerfile"
      via: "gosu installed in Dockerfile, used in entrypoint for UID remapping"
      pattern: "gosu"
---

<objective>
Update the container to support Unraid's PUID/PGID permission model, then generate the Unraid XML template through the Unraid Docker tab UI (not hand-written XML).

Purpose: This is the core deliverable of Phase 3 -- the XML template is what Unraid users interact with to install OpenClaw, and PUID/PGID support ensures file permissions work correctly on Unraid systems where the standard user is nobody:users (99:100) rather than node (1000:1000). Per research (Pitfall 5), templates MUST be generated by the Unraid Docker tab to ensure CA compatibility -- hand-written XML causes rejection.

Output: A Docker-tab-generated XML template file ready for Unraid template repositories, and an updated entrypoint that remaps container user UID/GID based on PUID/PGID environment variables.
</objective>

<execution_context>
@/Users/benglasser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benglasser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unraid-integration/03-RESEARCH.md
@.planning/phases/03-unraid-integration/03-CONTEXT.md
@Dockerfile
@entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update entrypoint and Dockerfile for PUID/PGID support</name>
  <files>entrypoint.sh, Dockerfile</files>
  <action>
The current container runs as `node` user (UID 1000, GID 1000). Unraid expects PUID=99/PGID=100 (nobody:users). The entrypoint must remap the node user's UID/GID at startup.

**Dockerfile changes:**
- Install `gosu` package in the apt-get install line (alongside dumb-init, curl, procps, git). gosu allows running commands as a different UID without the overhead/issues of su/sudo in containers.
- REMOVE the `USER node` line. The container must start as root to perform UID/GID remapping, then drop to the target user via gosu.
- Keep everything else the same (VOLUME, EXPOSE, HEALTHCHECK, ENTRYPOINT, CMD).

**entrypoint.sh changes:**
- At the top of the script (after set -e), add PUID/PGID defaulting:
  ```
  PUID=${PUID:-1000}
  PGID=${PGID:-100}
  ```
  Default PUID=1000 preserves backward compatibility for non-Unraid users. Default PGID=100 matches Unraid's `users` group.

- After the defaults, add UID/GID remapping logic:
  ```
  # Remap node user/group to match PUID/PGID
  if [ "$(id -u node)" != "$PUID" ]; then
    usermod -o -u "$PUID" node
  fi
  if [ "$(id -g node)" != "$PGID" ]; then
    groupmod -o -g "$PGID" node
  fi
  ```

- Update the chown of /home/node/.openclaw to use the new UID/GID (it already does `chown -R node:node` which will use the remapped UID/GID, so this is fine).

- Also chown /app to node:node to ensure the remapped user can read application files:
  ```
  chown -R node:node /home/node/.openclaw 2>/dev/null || true
  chown -R node:node /app 2>/dev/null || true
  ```

- Change the final `exec "$@"` to use gosu to drop privileges:
  ```
  exec gosu node "$@"
  ```

- Update the startup log to show PUID/PGID values:
  ```
  echo "PUID: $PUID, PGID: $PGID"
  ```

**Why gosu and not USER directive:** The USER directive is static. Unraid users configure PUID/PGID dynamically. The entrypoint must run as root to call usermod/groupmod, then drop to the remapped user via gosu. This is the standard pattern used by linuxserver.io images.

**Why not su/sudo:** gosu avoids TTY allocation issues and properly replaces the process (like exec). su/sudo create child processes that break signal handling with dumb-init.
  </action>
  <verify>
Build the Docker image: `docker build -t openclaw-test .` should succeed without errors.

Test default PUID/PGID:
```
docker run --rm openclaw-test id
```
Should show uid=1000(node) gid=100.

Test custom PUID/PGID:
```
docker run --rm -e PUID=99 -e PGID=100 openclaw-test id
```
Should show uid=99(node) gid=100.

Test that /home/node/.openclaw is owned by the remapped user:
```
docker run --rm -e PUID=99 -e PGID=100 openclaw-test ls -la /home/node/.openclaw
```
  </verify>
  <done>
Container starts as root, remaps node user to PUID/PGID values, drops to remapped user via gosu. Default PUID=1000 preserves non-Unraid compatibility. PUID=99/PGID=100 works for Unraid.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Generate XML template via Unraid Docker tab</name>
  <action>
The XML template MUST be generated by the Unraid Docker tab UI, not hand-written. Hand-written XML causes Community Applications rejection (Research Pitfall 5). The Unraid Docker tab generates proper XML structure that CA validators expect.

On your Unraid server:

1. **Navigate to Docker tab** in Unraid WebGUI

2. **Click "Add Container"** to open the template creation form

3. **Fill in these fields exactly:**

   **Basic fields:**
   - Name: `openclaw`
   - Repository: `benglasser/openclaw`
   - Network Type: `Bridge`
   - Privileged: `Off`
   - Overview/Description: `The AI that actually does things. OpenClaw is an AI gateway platform that manages agents, channels, and tool execution through an intuitive Control UI.`
   - WebUI: `http://[IP]:[PORT:18789]`
   - Icon URL: `https://raw.githubusercontent.com/OpenClaw/OpenClaw/main/packages/control/public/icon-512.png`
     (If this URL doesn't resolve, use any placeholder HTTPS PNG URL and note it for later)
   - Category: `Productivity:`
   - Support URL: `https://forums.unraid.net/topic/XXXXX-support-openclaw/` (placeholder -- Phase 4 updates)
   - Project URL: `https://github.com/OpenClaw/OpenClaw`

4. **Add Config entries** (click "Add another Path, Port, Variable, Label, or Device"):

   a. **Port** -- Container Port: `18789`, Host Port: `18789`, Connection Type: `TCP`
      Description: "Port for accessing the OpenClaw Control UI"

   b. **Path** -- Container Path: `/home/node/.openclaw`, Host Path: `/mnt/user/appdata/openclaw/`, Access Mode: `Read/Write`
      Description: "Stores OpenClaw configuration, agent data, and conversation history"

   c. **Variable** -- Key: `OPENCLAW_GATEWAY_TOKEN`, Value: (leave empty), Default: (leave empty)
      Show on: `Always`, Required: `No`, Mask: `Yes`
      Description: "Gateway authentication token (auto-generated if not set)"

   d. **Variable** -- Key: `ANTHROPIC_API_KEY`, Value: (leave empty), Default: (leave empty)
      Show on: `Always`, Required: `No`, Mask: `Yes`
      Description: "Claude API key for AI capabilities (can also be configured via Control UI after installation)"

   e. **Variable** -- Key: `PUID`, Value: `99`, Default: `99`
      Show on: `Advanced` (hidden by default), Required: `Yes`
      Description: "User ID for file permissions (Unraid default: 99)"

   f. **Variable** -- Key: `PGID`, Value: `100`, Default: `100`
      Show on: `Advanced` (hidden by default), Required: `Yes`
      Description: "Group ID for file permissions (Unraid default: 100)"

5. **Click APPLY** (or SAVE) to save the container configuration. Do NOT click "Create" yet if you don't want to actually run it -- just saving the template is sufficient.

6. **Export the generated XML:**
   - The auto-generated template XML is stored at: `/boot/config/plugins/dockerMan/templates-user/my-openclaw.xml`
   - Copy the contents of that file and provide it here, OR
   - Copy the file to a location accessible from your dev machine

Provide the XML content when resuming. Claude will validate and place it in the repository.
  </action>
  <resume-signal>Paste the auto-generated XML content, or provide the file path where it was copied</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate and commit the Docker-tab-generated XML template</name>
  <files>unraid-template/openclaw.xml</files>
  <action>
Take the XML provided by the user from Task 2 (generated by Unraid Docker tab) and:

1. Create the `unraid-template/` directory if it doesn't exist
2. Save the XML content to `unraid-template/openclaw.xml`
3. Validate the XML structure and verify it contains all required fields

**Validation checklist (grep/search for each):**
- `Container version="2"` -- proper schema version
- `<Repository>benglasser/openclaw</Repository>`
- `<WebUI>http://[IP]:[PORT:18789]</WebUI>`
- `<Category>Productivity:</Category>`
- `<Network>bridge</Network>`
- `<Privileged>false</Privileged>`
- Config entry with `Target="18789"` (port)
- Config entry with `Target="/home/node/.openclaw"` (path)
- Config entry with `Target="OPENCLAW_GATEWAY_TOKEN"` and `Mask="true"`
- Config entry with `Target="ANTHROPIC_API_KEY"` and `Mask="true"`
- Config entry with `Target="PUID"` and `Display="advanced-hide"` (or equivalent)
- Config entry with `Target="PGID"` and `Display="advanced-hide"` (or equivalent)

**If any fields are missing or incorrect:**
- Do NOT hand-edit the XML to fix structural issues (that defeats the purpose)
- Report what's missing to the user so they can re-generate via Docker tab
- Minor content fixes (typos in descriptions, URL corrections) are acceptable since they don't affect XML structure

**If all fields are present:**
- Save as-is to `unraid-template/openclaw.xml`
- The Docker-tab-generated XML may have slightly different formatting or additional fields compared to hand-written XML -- this is expected and correct
  </action>
  <verify>
Verify XML is well-formed:
```
xmllint --noout unraid-template/openclaw.xml
```
If xmllint not available, use: `python3 -c "import xml.etree.ElementTree as ET; ET.parse('unraid-template/openclaw.xml'); print('Valid XML')"`.

Verify required fields present:
- grep for Repository, Registry, WebUI, Category, Icon, Support, Network, Privileged
- grep for all 6 Config entries (Port, Path, 4 Variables)
- grep for Container version="2"
- grep for Mask="true" on OPENCLAW_GATEWAY_TOKEN and ANTHROPIC_API_KEY
- grep for Display="advanced-hide" (or equivalent) on PUID and PGID
  </verify>
  <done>
Valid Docker-tab-generated XML template exists at unraid-template/openclaw.xml with all required Unraid fields, 6 Config entries (port, volume, token, API key, PUID, PGID), proper Display levels, and masked sensitive variables. Template was generated by Unraid Docker tab UI, not hand-written.
  </done>
</task>

</tasks>

<verification>
1. Docker image builds successfully with gosu and PUID/PGID support
2. Container starts and remaps UID/GID correctly for both default and custom values
3. XML template was generated by Unraid Docker tab (not hand-written)
4. XML template is well-formed and contains all required Unraid template fields
5. Template Config entries have correct Types, Defaults, Display levels, and Mask settings
6. Volume mapping targets /home/node/.openclaw in container, defaults to /mnt/user/appdata/openclaw/ on host
7. WebUI field uses correct [IP]:[PORT:18789] format
</verification>

<success_criteria>
- docker build succeeds with gosu installed
- docker run with -e PUID=99 -e PGID=100 shows correct UID/GID remapping
- unraid-template/openclaw.xml was generated by Unraid Docker tab UI
- unraid-template/openclaw.xml passes XML validation
- Template contains Repository, WebUI, Category, Icon, Support, and all 6 Config entries
- Sensitive variables (token, API key) have Mask="true"
- PUID/PGID have Display="advanced-hide" with defaults 99/100
</success_criteria>

<output>
After completion, create `.planning/phases/03-unraid-integration/03-01-SUMMARY.md`
</output>
